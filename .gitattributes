# Normalize line endings: store text files with LF in the repository
* text=auto eol=lf

# Binary files (do not attempt to normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.gz binary
*.jar binary

# Shell scripts should have LF in repo
*.sh text eol=lf

# Ensure this file is stored with LF
.gitattributes text eol=lf
